# Functions

Functions are ways of creating reusable pieces of code that can be called.

**Rust**

```rust
fn create_user(id: usize, name: String) -> User {
   ...
}

let user = create_user(1, "Alice".to_string());
```

## Subsections

- [Function declarations](/functions/declarations)
- [Function calls](/functions/calls)
